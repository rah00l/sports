$('#countries').append("<%= j render 'countries', country: @countries %>");
<% if @countries.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @countries %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>