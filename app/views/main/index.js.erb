$('#sports').append("<%= j render 'main_containt' %>");
<% if @sports.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @sports %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>